backend:
  name: github
  repo: aay0909-rgb/Marathon_Project # 깃허브 저장소 주소 (경로 형식: 소유자/레포지토리)
  branch: main # 기본 배포 브랜치

# 이미지 업로드 설정 (로컬 및 CMS 업로드용)
media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "notice"
    label: "공지사항"
    folder: "data/notices"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    format: "json" # Markdown 대신 JSON 형태로 데이터를 저장하여 HTML 자바스크립트에서 바로 읽어오기 쉽게 만듦
    fields:
      - {label: "제목", name: "title", widget: "string"}
      - {label: "작성자", name: "author", widget: "string", default: "관리자"}
      - {label: "작성일", name: "date", widget: "datetime", format: "YYYY.MM.DD", date_format: "YYYY.MM.DD", time_format: false}
      - {label: "내용", name: "body", widget: "markdown"}

  - name: "registrations"
    label: "참가자 명단"
    file: "registrations.json" # 단일 파일
    format: "json"
    fields:
      - name: "registrants"
        label: "참가자"
        widget: "list"
        fields:
          - {label: "코스", name: "course", widget: "string"}
          - {label: "이름", name: "name", widget: "string"}
          - {label: "생년월일", name: "dob", widget: "string"}
          - {label: "성별", name: "gender", widget: "string"}
          - {label: "연락처", name: "phone", widget: "string"}
          - {label: "이메일", name: "email", widget: "string"}
          - {label: "비상연락처", name: "emergency_contact", widget: "string"}
          - {label: "티셔츠 사이즈", name: "tshirt_size", widget: "string"}
          - {label: "주문번호", name: "orderId", widget: "string", disabled: true}
          - label: "결제 상태"
            name: "paymentStatus"
            widget: "select"
            options:
              - {label: "입금 대기", value: "pending_transfer"}
              - {label: "입금 완료", value: "DONE"}
